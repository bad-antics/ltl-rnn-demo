// examples/demo.ltl — runnable demo for ltl_rnn_demo

import std::io
use ltl_rnn_demo as lib

fn main() {
    io::println("ltl_rnn_demo demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}